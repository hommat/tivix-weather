import styled from "styled-components";

export const SStatisticsCard = styled.li`
  margin-right: 2em;
  margin-top: 0.5em;
  min-width: 180px;
  border: 1px solid black;
  padding: 0.5em;

  &:last-child {
    margin-right: 0;
  }
`;

export const SHeading = styled.h3`
  text-align: center;
  margin-bottom: 0.5em;
`;

export const SListItem = styled.li`
  font-size: 0.95em;
`;
